import { PlaceholderScreen } from "@/components/PlaceholderScreen";

export default function StoresScreen() {
  return <PlaceholderScreen title="Stores" message="Store directory lands in Task 13." />;
}
